let initialState = {
   text: 'Settings Page in the development'
}

let SETTING_REDUSER = (state = initialState, action) => {
   switch(action.type){
      default: 
         return state

   }
}

export default SETTING_REDUSER